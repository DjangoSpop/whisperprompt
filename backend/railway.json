{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "backend/Dockerfile"
  },
  "deploy": {
    "startCommand": "python manage.py migrate && python manage.py seed_templates && gunicorn --bind 0.0.0.0:$PORT --workers 4 --timeout 120 ai_whisperer.wsgi:application",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  },
  "healthcheck": {
    "path": "/admin/",
    "timeout": 100
  }
}
